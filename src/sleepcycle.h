#ifndef SLEEPCYCLE_H
#define SLEEPCYCLE_H
// do another functions to make it more cool

// return a pointer to 4 diferent times and cycles to wake up
int timeToWakeUp(int (*array)[2], char const* time);

//return a pointer to 4 diferent times and cycles to fall asleep
int timeToFallAsleep(int (*array)[2], char const* time);


#endif
